<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-brand">CODE.VALID?</div>
        </div>

        <div class="collapse navbar-collapse" id="myNavbar">
            <% if current_user %>
            <div class="navbar-form navbar-right">
                <div class="form-group">
                    <div class="navbar-text">Signed in as
                        <%=" #{current_user.first_name} #{current_user.last_name}" %></div>
                </div>
                <div class="form-group">
                    <button type="button" class="btn btn-success">
                        <%= link_to 'Log Out', logout_path, method: :delete %></button>
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-success">
                        <%= link_to "Profile", user_path(current_user) %></button>
                </div>
            </div>
        <% else %>
            <div class="navbar-form navbar-right">

                <div class="form-group">
                    <button type="button" class="btn btn-success">
                        <%= link_to 'Sign Up', new_user_path %>
                    </button>
                </div>
                <div class="form-group">
                    <button type="button" class="btn btn-success">
                        <%= link_to 'Log In', login_path %>
                    </button>
                </div>
            </div>
        </div>
        <% end %>
    </div>
</div>
</nav>
