<%= form_for [@user, @post, @comment] do |c| %>

  <%= c.text_area :description, class: "form-control", placeholder: "Text here" %>
  <%= c.submit "Create comment", class: "btn btn-default link" %>


<% end %>
